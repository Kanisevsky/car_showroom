import React from 'react'
import { CustomButtonProps } from '@/app/types'
function CustomFilter({title}:CustomButtonProps) {
  return (
    <div>
      CustomFilters
    </div>
  )
}

export default CustomFilter
